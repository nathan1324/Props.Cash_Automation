# props-cash-automation configuration

# Base URL for props.cash (default: https://props.cash)
BASE_URL=https://props.cash

# Server port for the local UI (default: 3000)
PORT=3000

# Enable debug logging (default: false)
DEBUG=false

# Supabase / Postgres connection string (optional — scrapes still save to JSON/CSV without it)
# DATABASE_URL=postgresql://postgres:[PASSWORD]@db.[PROJECT].supabase.co:5432/postgres
